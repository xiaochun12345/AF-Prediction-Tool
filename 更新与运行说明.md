# 新版预测网站运行说明

更新日期：2026-09-19。模型版本：revision_v2_2026-09-19。

## 本次更新

- 主模型采用本轮主分析筛选出的49项变量，包括记录的COPD病程、11类入选药物及基线住院J44.1记录指标。ICS未入选，三项肺功能不进入网站主模型。
- 使用已训练的5套Elastic Net模型，分别计算生存概率后取平均；网站启动时不重新训练。
- 显示1、3、5、10年AF净风险。死亡按删失处理，这不是竞争风险累积发生概率。
- CHARGE-AF改为已发表的5年公式；HARMS₂-AF只报告分数。
- 修正HbA1c、尿酸单位，失眠、吸烟及跌倒次数编码；保留原有侧栏输入、绿色曲线和对照评分布局。
- 49项均须填写。点击“Load synthetic example”可加载合成示例，不能把示例值当作缺失数据的填补值。

## 核验

网站模型已与2091名内部验证及1417名时间验证参与者的冻结预测逐人核对；含缺失记录者按其各套填补数据分别核验。最大绝对数值误差约1.56×10⁻¹⁵。网站本身要求完整输入，不在线填补缺失数据。

输入检查、评分边界、示例计算、清空输入及修改输入后旧结果隐藏等11项自动测试通过。原始网站完整备份位于 `D:\copd-af\outputs\revision_v2\web_original_20260919`。

## 本机运行

在PowerShell运行：

```powershell
& 'D:\Users\Lenovo\anaconda3\envs\pycox\python.exe' -X utf8 'D:\copd-af\code\predictweb_final\run_local.py' --port 8766
```

打开 http://127.0.0.1:8766 。这是本机地址，不能作为论文中的公开网站地址。关闭运行窗口会停止本机服务。

其他电脑解压发布包后，先安装Python与依赖，再启动：

```sh
python -m pip install -r requirements.txt
python run_local.py
```

然后打开 http://127.0.0.1:8501 。依赖安装后，预测不依赖外部模型接口。

## 公网部署

仓库为 https://github.com/xiaochun12345/AF-Prediction-Tool ，应用入口为仓库根目录 `app.py`。部署到原Streamlit应用后沿用 https://af-prediction-tool.streamlit.app/ 。只有公网页面显示 `revision_v2_2026-09-19` 并通过示例预测检查，才能确认新版已生效。

主页面点击“Load synthetic example”，再点击“Calculate revised AF risk”，预期1、3、5、10年风险依次为0.45%、1.66%、3.40%、8.97%。这些只是合成示例，不代表某位参与者。

公开发布包包含网站源码、参数、合成示例及汇总核验报告，不包含参与者原始数据、逐人预测或填补器。应用用于研究演示，尚无独立外部验证。
